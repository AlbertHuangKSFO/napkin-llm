# CLAUDE.md — learning 库协作约定

> 给 Claude 读。短而密,只写反直觉的东西。每次踩到新坑,补一条进 Gotchas;别让它变长变空。

## 心智模型(最重要)
- 这是一张**网**,不是一棵树。层级靠 MOC + `[[双链]]` 表达,不靠嵌套文件夹。
- 文件夹只一层(领域):`深度学习基础/`、`LLM/`、`LLM Infra/`、`System Design/`、`Agent/`、`RAG/`、`AI 安全/`… 内部平铺,不建子文件夹。
- **编号前缀超 99 用三位**(`001`…`130`):`LLM/`、`LLM Infra/` 域已采用,保证文件管理器按序排列;两位数域(`Agent/`、`RAG/`、`深度学习基础/` ≤60)维持两位。
- **文件名冒号用全角 `：`**(全库一致,U+FF1A;**Windows 跨平台必须**,ASCII `:` 在 Windows `git clone` 非法):Obsidian 按 `：` 解析双链,双链 target 也用全角冒号。`/` 严禁(真路径分隔),枚举用全角 `、`。括号全角 `()`。
- 跨领域概念(如 KV-Cache)**只存一份**,放它第一次出现的领域文件夹,被多个 MOC 链接。

## 落位与命名
- 语言:中文。
- 原子笔记:一个文件只讲一个概念,便于被多个 MOC 复用。
- MOC 只放定位 + 链接,不写正文。
- 新笔记落进它所属领域的文件夹。
- **排序前缀**:`Agent/`、`RAG/` 内笔记用两位数前缀(`01 `、`02 `…)按 MOC 顺序排,让文件夹列表跟 MOC 一致;新增笔记接续编号。MOC 文件本身(`Agent.md`/`RAG.md`)不加前缀。

## 写文件:两个 server,根不同(必读,否则报错)
- 笔记 `.md` → 用 **mcpvault**,**库内相对路径**,例:`LLM/KV-Cache.md`。
- 图形 `.svg`/`.html` → 用 **filesystem**,**绝对路径**,且库比白名单根**深一层**:
  `/Users/jinze/Desktop/knowledgebase/learning/assets(别漏 `learning/`)。
- mcpvault 写不了 `.svg`/`.html`(白名单写死,不可配)。

## 图:优先手写 SVG,不用 Mermaid
- 默认用**精确布局的手写 SVG**。Jinze 嫌 Mermaid 丑、布局不可控,除非他明确要,否则不用 Mermaid。
- 复杂概念(架构、流程、数据流、状态机)**默认就该配图**,不要只给文字。
- **SVG 一律存进 `assets/`**(集中管理,领域文件夹只放 `.md`)。笔记里 `![[文件名.svg]]` 按**文件名全库解析**嵌入(静态 SVG 原生渲染,无需插件;因此移动 SVG 不会断图,但**文件名须全库唯一**)。
- 交互式 HTML 能写,但渲染需 **HTML Embed** 插件(目前未装),仅"必须拖参数才懂"时才上。

## Gotchas(默认不知道的坑)
- 文件路径必带 `learning/` 这一层(白名单根是它的父目录 `knowledgebase`)。
- 不碰 `.obsidian`、`.git`、`.DS_Store` 及任何点文件/点目录。
- filesystem 有覆盖/删除权限且护栏弱:写前确认路径。**注意:此库当前无 git**——批量改名/删除前先备份到库外,别指望 git 兜底。
- **重命名笔记 = 改文件名**,会断掉全库指向它的 `[[双链]]`;改名必须同步全库链接重写(Obsidian 外部改名不自动更新)。
- 库在 Desktop;若开了 iCloud 桌面同步,设"始终保留在此 Mac",否则文件被卸载、链接失效。
- 增加一些知识拓展

## 红线
- 删除 / 批量改写前先说明影响,等确认。
